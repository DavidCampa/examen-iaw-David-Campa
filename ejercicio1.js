const pt =require("prompt-sync")({signit: true})

let numInput
let num


const aleatorio = Math.round(Math.random()*10)
do { let numero = pt ("Cuantos dados quieres tirar: ")
console.log (numero)
if (Number(numInput)) {
    num = Number(numInput)
}
}
